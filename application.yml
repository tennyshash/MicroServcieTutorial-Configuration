spring:
    application:
      name: CONFIG-DEFAULT

eureka:
  client:
    register-with-eureka: true
    fetch-registry: true
    service-url:
      defaultZone: http://localhost:7003/eureka
  instance:
    prefer-ip-address: true
